<!DOCTYPE html>
<html lang="en">

<head>
    <!-- Meta tags for character set, compatibility, and viewport -->
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <!-- Internal styles using CSS -->
    <style>
        /* Reset and Base Styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Roboto', sans-serif;
        }

        /* CSS variables for color scheme and max width */
        :root {
            --primary-color: #a855f7;
            --primary-color-dark: #9333ea;
            --secondary-color: #ca8a04;
            --text-dark: #1f2937;
            --text-light: #6b7280;
            --extra-light: #faf5ff;
            --max-width: 1200px;
        }

        /* Navigation styles */
        nav {
            width: 100%;
            position: fixed;
            top: 0;
            left: 0;
            background-color: #ffffff;
            z-index: 1;
        }

        .nav__content {
            max-width: var(--max-width);
            margin: auto;
            padding: 1.5rem 1rem;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }

        nav .logo a {
            font-size: 1.5rem;
            font-weight: 600;
            color: var(--primary-color);
            transition: 0.3s;
        }

        nav .logo a:hover {
            color: var(--primary-color-dark);
        }

        /* ... (continued styles for navigation and other elements) ... */

        /* JavaScript for accordion functionality */
        .accordion {
            cursor: pointer;
            padding: 1.8vh 2.5vw;
            width: 100%;
            text-align: left;
            border: none;
            outline: none;
            transition: 0.4s;
            font-size: 2.2vh;
            font-weight: bold;
            border-radius: 5px;
        }

        .accordion.active,
        .accordion:hover {
            background-color: rebeccapurple;
            color: white;
        }

        .accordion:after {
            content: '\002B';
            color: #777;
            font-weight: bold;
            float: right;
            margin-left: 5px;
        }

        .accordion.active:after {
            content: "\2212";
        }

        .panel {
            padding: 0 2.5vw;
            display: none;
            overflow: hidden;
        }

        .show {
            display: block;
        }
    </style>

    <!-- JavaScript for toggling accordion panels -->
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            const accordions = document.querySelectorAll(".accordion");

            accordions.forEach(accordion => {
                accordion.addEventListener("click", function () {
                    const panel = this.nextElementSibling;
                    panel.classList.toggle("show");
                });
            });
        });
    </script>

    <!-- Page title -->
    <title>About Us</title>
</head>

<body>
    <!-- Navigation bar -->
    <nav>
        <div class="nav__content">
            <div class="logo"><a href="#">Anuj Jaglan</a></div>
            <!-- ... (continued navigation elements) ... -->
        </div>
    </nav>

    <!-- Main content wrapper -->
    <div class="wrapper">
        <!-- Background container with two elements -->
        <div class="background-container">
            <div class="bg-1"></div>
            <div class="bg-2"></div>
        </div>

        <!-- About container with information about the person -->
        <div class="about-container">
            <!-- Heading and paragraphs with information about the person -->
            <h1>About Me </h1>
            <p>
                <!-- ... (continued paragraphs with personal information) ... -->
            </p>

            <!-- Accordion section with reasons to work with the person -->
            <div class="text-container">
                <h3>Why work with me</h3>
                <br>
                <button class="accordion">1. Expertise</button>
                <div class="panel">
                    <div class="privacy-content">
                        <p>
                            <!-- ... (content about expertise) ... -->
                        </p>
                    </div>
                </div>
                <!-- ... (additional accordion sections) ... -->
            </div>

            <!-- Final section with a call to action and closing statements -->
            <p>
                <h3>Join Me on the Road Ahead</h3>
                <!-- ... (closing statements and call to action) ... -->
            </p>
        </div>
    </div>
    </div>
</body>

</html>
